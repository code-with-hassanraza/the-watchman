# Lambda deployment package
function.zip

# Python cache
__pycache__/
*.pyc
*.pyo

# Environment files (never commit these)
.env
*.env

# AWS credentials (never commit these)
credentials
.aws/

# Local test overrides with real values
test-event-real.json